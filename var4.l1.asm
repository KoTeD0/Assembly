.model small

.stack 100h

.data
string db 'edcbabbffccfgaabrrzz', 0AH, '$' ; ??????, ??? ??????? ???????????? ????????
len dw 20 ; ????? ??????
.code

start:
mov ax, DGROUP
mov ds, ax ; ??????? ?????? ? Data Segment
mov dx, [len]
mov cx, dx ; ??????? ????? ?????? ? ??????? cx (??????? ??? ??????)
jmp lb3 ; ?????????? ??????? ?? ???? lb3
lb2: ; ?????? ??????? ???????
dec di
mov [string[di]], bl
inc di
mov [string[di]], al

loop lb ; ????????? ? ????? lb
lb3: ; ???????? ????
mov di, 0
mov dx, cx
mov cx, [len]
sub cx, 1
lb: ; ???? ?????????? ??????????? n ? n + 1
mov al, string[di] ; ?????? ???????? n ? ??????? al
inc di
mov bl, string[di] ; ?????? ???????? n+1 ? ??????? bl
cmp al, bl ; ????????? al ? bl
jg lb2 ; ???? al > bl ?? ASCII ?????????? ??????? ?? lb2
loop lb
mov cx, dx
loop lb3
; ?????

mov ah, 9
mov dx, offset string
int 21h
mov ax,4C00h
int 21h
end start